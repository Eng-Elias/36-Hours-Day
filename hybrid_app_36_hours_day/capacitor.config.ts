import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hybrid_app_36_hours_day',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
